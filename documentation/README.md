# Running docs:

make dev → hot-reload at http://localhost:3000
make build → static site in documentation/build/ (ready for GitHub Pages or any static host)
make deploy → deploys to GitHub Pages using the organizationName/projectName in the config