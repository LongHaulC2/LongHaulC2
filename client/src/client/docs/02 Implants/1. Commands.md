Here is the updated markdown documentation, organized by logical groups with headers and separators.

# Implant Task Command Reference

This document outlines the available commands for the implant, grouped by functionality.

---

## 1. System Commands

Control the state and execution of the implant process itself.

| Command | Description |
| --- | --- |
| **`exit`** | Terminate the implant process on the host. |
| **`sleep`** | Sleep for a specified number of seconds on the host. |

### Detailed Usage

#### `exit`

Exits the implant execution.

* **Note:** This kills the implant process on the host. Do not expect a response confirmation from the implant after this command is issued.
* **Usage:** `exit`

#### `sleep`

Commands the implant to sleep for a specified number of seconds.

* **Usage:** `sleep <seconds>`
* **Example:**
```bash
sleep 5

```



---

## 2. File System Operations

Interact with files on the host's disk.

| Command | Description |
| --- | --- |
| **`cd`** | Change the current working directory on the host. |
| **`ls`** | List the contents of a directory on the host. |
| **`file download`** | Download a file from the host machine to the server. |
| **`file upload`** | Upload a file from the server to the host machine. |

### Detailed Usage

#### `cd`

Changes the current working directory on the host machine.

* **Usage:** `cd <directory>`
* **Example:**
```bash
cd C:\Users\

```



#### `ls`

Lists the contents of a directory on the host. If no directory is provided, it defaults to the current working directory (`.`).

* **Usage:** `ls [directory]`
* **Example:**
```bash
ls C:\Users\

```



#### `file download`

Retrieves a file from the host machine where the implant is running.

* **Usage:** `file download <file_path>`
* **Example:**
```bash
file download C:\Users\user\file.txt

```



#### `file upload`

Uploads a file to the host machine. Requires the file contents to be encoded in Base64 (typically handled by the client CLI).

* **Usage:** `file upload <file_path> <base64_content>`
* **Example:**
```bash
file upload C:\Users\user\file.txt <base64_file_contents>

```



---

## 3. C2 Strategy

Manage how the implant communicates with the C2 server.

| Command | Description |
| --- | --- |
| **`strat post`** | Set the "post" strategy (data exfiltration method). |
| **`strat get`** | Set the "get" strategy (task retrieval method). |
| **`strat list`** | List all available strategies on the implant. |
| **`strat active`** | List the currently active strategies. |

### Detailed Usage

#### `strat post`

Sets the posting strategy (how the implant sends data back).

* **Usage:** `strat post <strategy_name>`
* **Example:**
```bash
strat post my_post_strategy

```



#### `strat get`

Sets the getting strategy (how the implant retrieves tasks).

* **Usage:** `strat get <strategy_name>`
* **Example:**
```bash
strat get my_get_strategy

```



#### `strat list`

Lists the available strategies configured on the implant.

* **Usage:** `strat list`

#### `strat active`

Displays the currently active strategies.

* **Usage:** `strat active`

---

## 4. Memory Store

Interact with the implant's volatile memory storage (RAM-only file system).

| Command | Description |
| --- | --- |
| **`memstore list`** | List all filenames currently in the memory store. |
| **`memstore upload`** | Upload a file into the implant's internal memory store. |
| **`memstore download`** | Download a file from the implant's internal memory store. |
| **`memstore delete`** | Delete a specific file from the memory store. |
| **`memstore clear`** | Clear *all* files from the memory store. |

### Detailed Usage

#### `memstore list`

Lists the names of all files currently stored in the memory store.

* **Usage:** `memstore list`

#### `memstore upload`

Uploads a file directly to the implant's volatile memory store (RAM). Useful for staging files without touching the disk.

* **Usage:** `memstore upload <file_name> <base64_content>`
* **Example:**
```bash
memstore upload my_tool.exe aabbcc==

```



#### `memstore download`

Retrieves a file from the implant's memory store.

* **Usage:** `memstore download <file_name>`
* **Example:**
```bash
memstore download example.txt

```



#### `memstore delete`

Removes a specific file from the implant's memory store.

* **Usage:** `memstore delete <file_name>`
* **Example:**
```bash
memstore delete not_more_malware

```



#### `memstore clear`

Wipes all files from the memory store.

* **Usage:** `memstore clear`