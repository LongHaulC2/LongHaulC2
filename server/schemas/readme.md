# schemas

This is where data schemas go, ex, dataclasses for the API inputs.  DB definitions DO NOT go here, put that in the `db` folder.
These are often used as API responses, so this is where the inpupt & output "contracts" for data go. 

Additinally, these classes are used as inputs for the DB schema. These are your source of truth for how data is defined with the API.