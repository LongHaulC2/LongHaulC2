# db

This is where all the db definition and connector code goes. It's a seperate folder from modules, as it includes server logic, 
and definitions/schemas for the DB's. Generally I like to keep DB stuff in a seperate folder, as it's an "external" thing to the rest of the code.