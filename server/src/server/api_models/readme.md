# The API models. These are used for marshalling the inputs, and outputs for the API.

Each endpoint has their own models

These are the FINAL. SAY. in what the API spits out. If you're trying to remove a response (ex, an internal ID field from a DB), edit it out in the response model. 